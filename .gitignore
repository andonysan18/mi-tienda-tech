# Dependencias (Pesadas, nunca se suben)
node_modules/
package-lock.json 
# (Nota: package-lock a veces sí se sube, pero si quieres evitar conflictos trabajando solo, puedes ignorarlo por ahora. Lo estándar es SÍ subirlo, pero node_modules JAMÁS).

# Next.js build output
.next/
out/
build/
dist/

# Variables de entorno (SECRETOS - NUNCA SUBIR)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs del sistema
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# Editor
.vscode/
.idea/
*.swp
.DS_Store